class Note {
  String subject;
  String title;
  String description;
  DateTime createdAt;

  Note({
    required this.subject,
    required this.title,
    required this.description,
    required this.createdAt,
  });
}
